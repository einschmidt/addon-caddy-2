---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "55 2 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  workflows:
    uses: einschmidt/workflows/.github/workflows/lock.yaml@main
